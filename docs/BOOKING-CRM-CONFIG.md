# Booking CRM configuration

The CRM now includes an admin-only Booking Setup section. It manages `booking_settings` and `booking_schedule_rules` directly in Supabase. Run `supabase/sql/BOOKING-SETTINGS-SETUP.sql` first.
